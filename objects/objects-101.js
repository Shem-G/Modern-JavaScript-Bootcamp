let myBook = {
    title: 'Erty Adentures',
    author: 'Hrerj',
    pageCount: 42069
}

console.log(`${myBook.title} by ${myBook.author} which has ${myBook.pageCount} pages`)

myBook.title = 'Fattigook'

console.log(`${myBook.title} by ${myBook.author} which has ${myBook.pageCount} pages`)

// Challenge area
// name, age, location
// [name] is [age] and lives in [location]
// Increase age by 1 and print message again

let person = {
    name: 'Yuki Tuki',
    age: 4,
    location: 'Hrooken Realm'
}

console.log(`${person.name} is ${person.age} and lives in ${person.location}`)

person.age = 69

console.log(`${person.name} is ${person.age} and lives in ${person.location}`)